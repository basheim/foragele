import { useEffect, useState } from 'react';
import styles from '../../styles/Navigation.module.css';

export interface MobileBarProps {
}

const MobileBar = ({ }: MobileBarProps) => {

  return (
    <div>
    </div>
  )
}

export default MobileBar;
